<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.babel.web.system.dao.MenuDao">
    <!--目的：为dao接口方法提供sql语句配置-->
    <insert id="addMenu"  parameterType="com.babel.web.system.po.MenuPo">
        INSERT INTO menu (guid,
                          menu_name,
                          resource_guid,
                          parent_guid,
                          menu_index,
                          icon_class)
              VALUES (#{guid},
                    #{menuName},
                    #{resourceGuid},
                    #{parentGuid},
                    #{menuIndex},
                    #{iconClass})
    </insert>

    <delete id="deleteMenu" parameterType="String">
     DELETE FROM menu WHERE guid = #{guid}
    </delete>

    <update id="reduceNumber">
        UPDATE menu
        SET menu_name = #{menu_name},
            resource_guid = #{resource_guid},
            parent_guid = #{parent_guid},
            menu_index = #{menu_index},
            icon_class = #{icon_class}
        WHERE
            guid = #{guid}
        AND number > 0
    </update>
    
    <resultMap id="menuPo" type="com.babel.web.system.po.MenuPo">
        <id property="guid" column="guid"/>
        <result property="menuName" column="menu_name"/>
        <result property="parentGuid" column="parent_guid"/>
        <result property="menuIndex" column="menu_index"/>
        <result property="iconClass" column="icon_class"/>
        <association property="tempClass" javaType="com.babel.web.system.po.ResourcePo">
            <id property="resourceGuid" column="resource_guid"/>
            <result property="resource" column="resource"/>
        </association>
    </resultMap>
    
    <select id="queryAllMenus" resultType="com.babel.web.system.po.MenuPo">
        SELECT
            m.guid,
            m.menu_name,
            m.resource_guid,
            m.parent_guid,
            m.menu_index,
            m.icon_class,
            r.resource
        FROM
            menu m
        LEFT JOIN
            resources r
        ON
            m.resource_guid = r.guid
    </select>

</mapper>